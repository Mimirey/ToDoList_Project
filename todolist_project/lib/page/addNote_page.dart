import 'package:flutter/material.dart';

class AddnotePage extends StatelessWidget {
  const AddnotePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Ini add page"),
    );
  }
}